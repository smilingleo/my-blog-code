{
  "name": "smilingleo-blog",
  "description": "My personal blog site.",
  "version": "1.0.0",
  "author": "Leo Liu <leo.wei.liu@gmail.com>",
  "dependencies": {
    "gatsby": "^5.4.0",
    "gatsby-link": "^1.6.17",
    "gatsby-paginate": "^1.0.12",
    "gatsby-plugin-react-helmet": "^1.0.5",
    "gatsby-source-filesystem": "^5.4.0",
    "gatsby-transformer-remark": "^6.4.0",
    "react-disqus-thread": "^0.4.0"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "deploy": "gatsby build --prefix-paths && node_modules/.bin/gh-pages -d public",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "prettier": "^1.6.1"
  }
}
